# -----------------------------------------------------
# CUSTOMIZATION
# -----------------------------------------------------
# POSH=agnoster

# -----------------------------------------------------
# Prompt
# -----------------------------------------------------
# eval "$(oh-my-posh init bash --config /usr/share/oh-my-posh/themes/cloud-context.omp.json)"
ln -sf ~/.config/starship/bracketed-segments.toml ~/.config/starship.toml
eval "$(starship init bash)"
