# __        __       __  _
# \ \      / /___   / _|(_)
#  \ \ /\ / // _ \ | |_ | |
#   \ V  V /| (_) ||  _|| |
#    \_/\_/  \___/ |_|  |_|
#
# banner ganerate by http://www.network-science.de/ascii/
# use font style standard

# 菜单宽度，默认占用屏幕50%，携带%后缀未屏幕占比，否则默认像素点
# width=70%
# 菜单高度，默认占用屏幕50%，携带%后缀未屏幕占比，否则默认像素点
# height=400
# 搜索框提示内容
prompt=键入以筛选内容
# 横向偏移量
# xoffset=0
# x=0
# 纵向偏移量
# yoffset=0
# y=0
# 使用默认窗口打开wofi，而不是xlr-layer-shell
# normal_window=true
# 处理和渲染图像转义序列
allow_images=true
# 处理和渲染pango标记
allow_markup=true
# 缓存文件路径
# cache-file=$XDG_CACHE_HOME/wofi-<mode>
# 指定在终端中运行程序时要使用的术语
term=kitty
# wofi密码模式显示的字符
# password=
# 使用enter执行搜索，而不是使用默认第一个匹配的选项
# exec_search=true
# 隐藏滚动条
hide_scroll=true
# 匹配模式，默认contains。支持contains, strict-contains, multi-contains, fuzzy
# matching=MODE
# 匹配忽略大小写
insensitive=true
# 显示位置
# location=center
# 禁用多功能选项，如应用菜单右键选项将被隐藏
no_actions=true
# 指定显示几行
# lines=
# 指定显示几列
columns=2
# 使用gtk黑暗模式，如果gkt黑暗模式可用
gtk_dark=true
# 打开wofi时默认填充搜索框以默认搜索内容
# search=
# 打开wofi时显示在哪个显示器
# monitor=eDP-1
# 指定一个类似 printf 的字符串，该字符串用于之前的条目显示它们。 此命令仅用于表示标签小部件的string，并且不会影响所选标签的输出。
# pre_display_cmd=COMMAND
# 指定搜索结果的更新速率（以毫秒为单位）
# filter_rate=100
# 指定启用图像时图像的大小（以像素为单位）
# image_size=32
# 隐藏搜索框
# hide_search=true
# 当失去焦点隐藏wofi
close_on_focus_loss=true
# 指定是否应动态缩小 wofi 以适应 可见线或是否应始终保持相同的大小。
# dynamic_lines=true
# 指定要打开的图层。支持background（后台启动）, bottom（所有窗口下）, top（所有窗口上）, overlay（堆叠），默认top
# layer=top
always_parse_args=true
show_all=false
print_command=true
